pragma solidity ^0.4.21;

contract vegetable {
    function () public;
    // TODO: remove this "fake" when Populus supports calling the "fallback"
    function fake() public view returns(bytes32 _data);
}
