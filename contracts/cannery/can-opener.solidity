pragma solidity ^0.4.21;

contract can_opener {
    function open(address _can) public;
    function open(address _can, bytes _data) public;
    event opened(address indexed _caller, address indexed _can, address _vegetable);
}
